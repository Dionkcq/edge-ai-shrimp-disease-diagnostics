# Python
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.venv/
venv/

# Node, browser-test and generated web output
frontend/node_modules/
frontend/dist/
frontend/coverage/
frontend/playwright-report/
frontend/test-results/

# Notebooks
.ipynb_checkpoints/

# Secrets and local settings
.env
.env.*
*.key
*.pem

# Large data and model artifacts
private/
data/raw/
data/processed/
data/mapping_acceptance.json
runs/
artifacts/
model/training/runs/
model/training/returns/
model/training/work/

# Model weights are never committed, wherever they land. These patterns are deliberately
# repository-wide rather than scoped to one directory: the training toolchain downloads its own
# assets into the working directory, so weights appear outside any anticipated location.
# No weight file has ever been tracked, so these rules untrack nothing.
*.pt
*.pth
*.onnx
*.tflite
*.engine

# OS/editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
